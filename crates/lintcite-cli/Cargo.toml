[package]
name = "lintcite-cli"
description = "The lintcite CLI: a thin shell over the SDK (plan 06; no linting logic lives here)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "lintcite"
path = "src/main.rs"

# Surfaces depend on the SDK facade and nothing below it (P1).
[dependencies]
lintcite = { path = "../lintcite" }
