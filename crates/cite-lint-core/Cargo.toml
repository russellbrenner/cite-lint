[package]
name = "cite-lint-core"
description = "Deterministic citation engine: lexer, parser, typed AST, IR rule engine (invariants 1, 3, 4)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# Invariant 1: core depends ONLY on data. Adding cite-lint-host or any
# surface here must fail review and the dependency-graph test.
[dependencies]
cite-lint-data = { path = "../cite-lint-data" }
