[package]
name = "cite-lint-lsp"
description = "The cite-lint LSP server (M1 stub; the live-drafting surface lands per plan 06 T3)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "cite-lint-lsp"
path = "src/main.rs"

# Surfaces depend on the SDK facade and nothing below it (P1).
[dependencies]
cite-lint = { path = "../cite-lint" }
